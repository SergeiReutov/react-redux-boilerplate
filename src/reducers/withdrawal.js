const initialState = {
  cash: [],
  isLoading: false,
  error: '',
};

export default function withdrawal(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
